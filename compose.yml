services:
  fdp_contest:
    environment:
      # Поменять на актуальный адрес хоста
      - HOST_IP=192.168.19.194
    volumes:
      - ./data:/app/data
    build: .
    ports:
      - "80:80"
    restart: always
